import 'package:flutter/material.dart';

class PrintActionBar extends StatelessWidget {
  const PrintActionBar({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
